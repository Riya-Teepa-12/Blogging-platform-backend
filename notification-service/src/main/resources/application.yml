server:
  port: 8087

spring:
  application:
    name: notification-service
  datasource:
    url: ${NOTIFICATION_DB_URL:jdbc:h2:file:./data/notificationdb;MODE=MySQL;AUTO_SERVER=TRUE}
    username: ${NOTIFICATION_DB_USERNAME:sa}
    password: ${NOTIFICATION_DB_PASSWORD:}
    driver-class-name: ${NOTIFICATION_DB_DRIVER:org.h2.Driver}
  jpa:
    database-platform: ${NOTIFICATION_JPA_DIALECT:org.hibernate.dialect.H2Dialect}
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: false
